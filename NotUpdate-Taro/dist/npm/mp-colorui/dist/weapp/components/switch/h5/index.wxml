<block wx:if="{{$taroCompReady}}">
    <block>
        <block wx:if="{{type === 'form'}}">
            <view class="{{anonymousState__temp3}}" style="{{anonymousState__temp4}}">
                <view class="title">{{title}}</view>
                <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}" bindtap="anonymousFunc0">
                    <view class="{{'h5-switch-input sm ' + shapeClassName}}"></view>
                </view>
            </view>
        </block>
        <block wx:else>
            <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}" bindtap="anonymousFunc0">
                <view class="{{'h5-switch-input sm ' + shapeClassName}}"></view>
            </view>
        </block>
    </block>
</block>