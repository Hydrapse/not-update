<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <view class="{{anonymousState__temp3}}">
            <block wx:if="{{props.title && props.title.text}}">
                <view class="cu_card__title-line padding">
                    <cl-text compid="{{$compid__81}}"></cl-text>
                </view>
            </block>
            <slot name="title"></slot>
            <view class="padding">
                <slot></slot>
            </view>
        </view>
    </view>
</block>