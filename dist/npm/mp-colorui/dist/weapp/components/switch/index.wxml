<block wx:if="{{$taroCompReady}}">
    <block>
        <block wx:if="{{anonymousState__temp5}}">
            <cl-switch-h-5 compid="{{$compid__83}}"></cl-switch-h-5>
        </block>
        <block wx:else>
            <block>
                <block wx:if="{{type === 'form'}}">
                    <view class="{{anonymousState__temp3}}" style="{{anonymousState__temp4}}">
                        <view class="title">{{title}}</view>
                        <switch class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}" checked="{{checked}}" bindchange="anonymousFunc0" disabled="{{props.disabled}}"></switch>
                    </view>
                </block>
                <block wx:else>
                    <switch class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}" checked="{{checked}}" bindchange="anonymousFunc0" disabled="{{props.disabled}}"></switch>
                </block>
            </block>
        </block>
    </block>
</block>