<block wx:if="{{$taroCompReady}}">
    <view class="{{current == '0' ? 'index' : 'white-index'}}">
        <view class="navbar" style="{{anonymousState__temp}}"></view>
        <block>
            <block wx:if="{{current == '0'}}">
                <main></main>
            </block>
            <block wx:else>
                <user></user>
            </block>
        </block>
        <at-tab-bar compid="{{$compid__44}}"></at-tab-bar>
    </view>
</block>